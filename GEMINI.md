# GEMINI.md

Read `AGENTS.md` first. It is the single source of instructions for every agent in this
repository, including Google Antigravity and Jules.

Antigravity: pick work packages labelled `agent:antigravity`. Jules: pick issues labelled
`agent:jules` (plus the `jules` trigger label). Never edit the protected files listed in
AGENTS.md section 3; never commit to `main`; one branch `wp/NN-<slug>` per work package.
